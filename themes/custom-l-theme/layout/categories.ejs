<section class="categories-page">
  <div class="container">
    <header class="page-header">
      <h1>文章分类</h1>
      <p>共 <%= site.categories.length %> 个分类</p>
    </header>

    <div class="categories-content">
      <% if (site.categories.length > 0) { %>
        <div class="category-grid">
          <% site.categories.forEach(cat => { %>
            <div class="category-card">
              <div class="category-icon">📁</div>
              <h3 class="category-name">
                <a href="<%= url_for(cat.path) %>"><%= cat.name %></a>
              </h3>
              <span class="category-count"><%= cat.posts.length %> 篇文章</span>
              <ul class="category-posts">
                <% cat.posts.slice(0, 3).forEach(post => { %>
                  <li><a href="<%= url_for(post.path) %>"><%= post.title %></a></li>
                <% }) %>
                <% if (cat.posts.length > 3) { %>
                  <li><a href="<%= url_for(cat.path) %>" class="view-all">查看全部 →</a></li>
                <% } %>
              </ul>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <div class="empty-state">
          <p>暂无分类</p>
        </div>
      <% } %>
    </div>
  </div>
</section>